name: sewage_management_system
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.0.5
  http: ^1.2.2  # Ensure this is compatible with your app requirements
  google_maps_flutter: ^2.1.1  # Google Maps Flutter dependency
  firebase_core: ^2.10.0  # Add Firebase core package
  firebase_auth: ^4.3.0  # Add Firebase authentication package if you need authentication

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/Green.jpg



