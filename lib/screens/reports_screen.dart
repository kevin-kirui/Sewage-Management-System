import 'package:flutter/material.dart';

class ReportsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Reports & Insights')),
      body: Center(
        child: Text('Reports and insights will be displayed here.'),
      ),
    );
  }
}
